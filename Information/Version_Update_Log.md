# 版本开发日志

## v1.3.0 LTS
**2026 年 8 月 21 日**

v1.3.0 参加飞书 AI 绝活黑客松，接入飞书机器人，内置多维表格分析功能，同时优化部分功能。原有本地数据源、分析工具和 Web 工作台均保留。

### 不必导出表格，也能在群里做经营分析

项目负责人经常面临同一个问题：任务、成本和风险都在多维表格中，真正的判断却要经历“导出数据、另行分析、再把结论复制回群”的多次切换。现在，在飞书群内 @ 智析 Agent 并附上多维表格链接，就可以直接提问：

- 哪些任务风险最高，下一步应该优先处理什么？
- 成本偏差主要集中在哪个项目、区域或负责人？
- 谁的任务负载异常，需要怎样协调？

机器人将结论回到同一协作群，成员不需要离开讨论现场。

### 飞书和 AI 各自真正完成关键工作

这不是将一段 AI 文案转发到飞书。

- **飞书**负责接收群内问题、读取多维表格中的真实记录、承载团队讨论，并在需要时写入新的结果表或更新指定记录。
- **AI**负责理解业务问题，将表格作为 SQL 数据源选择分析路径，完成查询、归因、图表/报告生成并给出行动建议。

少了飞书，AI 无法获得团队正在协作的数据和把结果交回现场；少了 AI，表格也无法自动形成针对问题的分析和建议。

### 从群聊到结果表的可见闭环

1. 在 Web 对话中使用 `/robot` 选择已加入群聊的机器人，顶部出现绿色连接状态。
2. 在飞书群内 @ 机器人并发送分析问题和多维表格链接。
3. 机器人读取表格，分析过程与结果同步到 Web 对话，并把结论回复到群内。
4. 当成员明确要求“新建风险周报”或“写入处理建议”时，机器人创建结果表、追加记录或更新指定记录，并返回真实的飞书链接。

手机和桌面飞书发送的消息都能进入同一分析会话。对于飞书不支持的 Web 选项交互，机器人会改为可直接回复序号的文本，保证群内流程不中断。

### 写回前保留人的决定权

读取和分析可以由机器人完成，但任何会改变协作数据的操作都必须由成员明确提出。系统不会在分析结束后自动创建表、追加记录、修改数据或调整权限；缺少表格协作者权限、机器人入群或应用授权时，会明确报错而不伪造成功结果。

### 为团队复用而设计

- 飞书多维表格被作为标准数据源接入既有 DuckDB / SQL 分析链路，因此可以复用已有的查询、分析、图表与报告能力。
- 内置的多维表格分析流程将“读取 → SQL → 分析/报告 → 明确写回”固定为可复用步骤，其他团队可按同样的机器人、表格和群聊配置跑通。
- 本地使用时默认通过飞书长连接接收消息，无需将 `localhost` 暴露到公网；部署到服务器时也支持 Webhook。

### 本次新增与演示证据

本版本新增飞书机器人渠道、群聊与 Web 会话双向同步、飞书多维表格读写、受控写回和移动端文本交互降级。Demo 使用 240 条脱敏/模拟的项目交付数据，现场展示风险、成本偏差和负责人负载分析；模拟数据只用于复现流程，不代表客户数据或业务成效。

飞书机器人、Bitable 服务、工具注册和分析流程的专项测试共 53 项通过，前端构建检查通过。最终演示以实际运行环境中完成“群内提问、完整回复、读表、返回真实结果表链接”的一镜到底结果为准。

### 设置体验与后续能力

- “机器人”是可扩展的渠道入口，飞书为当前可用渠道；其他渠道将随着实际可用性逐步加入。
- LLM 模型页已修复内置提供商行的双层内缩、悬停背景不完整、区块留白过大以及长配置被底部操作区裁切的问题。
- GPU 算力暂标记为“规划中”，避免将尚未完成端到端验收的能力误呈现为可用；相关实现会保留，成熟后再开放。

### 长期记忆：更稳定，也更可控

本版本修复了思考模型把推理文本混入输出、模型返回 JSON 格式不规范，以及“用户偏好”和“当前项目事实”被误判等导致的记忆提取失败。现在，系统会更稳妥地提取可长期复用的偏好与上下文，并在提取或整理完成后提示结果。

你可以在“设置 → 通用”中一键关闭长期记忆；关闭后，新的对话不会读取、提取或整理长期记忆。系统也会定期合并重复或过时的记忆，并在恢复较久以前的会话时保护不完整的工具调用记录和过长上下文，减少恢复失败或上下文膨胀。

### 知识库检索：状态不再“看起来可用”

- 修复本地 Embedding 模型的下载与 ONNX 兼容问题，使本地语义检索可以正常恢复。
- 云端检索不再因为“尚未检测”就显示为可用；用户可主动检查连接状态，失败时会明确显示不可用并按既有策略回退。
- 原“Hash”检索模式在界面中更名为“基础”，含义更直观，内部兼容性保持不变。

### 分析工作台与兼容性

- 宽屏下的聊天内容区会随窗口宽度合理扩展，避免消息和输入区不必要地过窄。
- 完善 SQL Server 的驱动、超时、错误分类与大表使用边界，降低首次连接和远程查询的排障成本。
- 工作流运行的模型、工具调用、耗时和成本信息得到进一步整理，便于后续追溯和审计。

## v1.2.0 LTS
**2026年7月17日**

本次长期支持版本聚焦 **更顺畅的协作分析、可编辑的商业图表，以及更可靠的数据处理体验**。

### 1. AI 团队协作升级
- 将一个复杂目标拆分为可追踪的协作任务，清楚展示进度、结果与下一步
- 可先确认执行计划，再按依赖关系推进；遇到需要人工决定的内容会明确提示
- 支持质量复核与针对性重试，让需要调整的环节可以单独处理，不必从头开始

### 2. 全新商业画布
- 新增商业模式画布、BCG 矩阵、SWOT 分析和空白画布，帮助把分析结论快速整理成图
- 可直接在应用内编辑图形、保存项目、重命名、回看和导出图片或 `.drawio` 文件

### 3. 更贴合业务的分析助手
- 技能会根据你的问题自动匹配，也可以创建和编辑自己的分析流程
- 支持生成可离线打开的交互式业务看板，便于分享分析结果
- 数据读取与分析提示更完善，减少字段、口径和图表不匹配带来的困扰

### 4. 更可靠、更高效的处理体验
- 大型任务与多项操作的处理更流畅，等待期间仍可清楚了解进度
- 工作目录中的常见文档和文件夹内容更容易读取与使用
- 数据分析结果增加了更清晰的质量提示和可核查依据，帮助判断结论是否可靠

### 5. 界面与使用体验改进
- 存储设置和窄屏界面布局更清晰，信息与操作不再拥挤
- 检查更新后可直接前往 GitHub Releases 选择所需安装包

### 6. 本地数据清理更安心
- 在“设置 → 存储”中查看本地数据占用、可清理候选和回收站内容
- 不再需要的上传文件和历史产物可先移入回收站；恢复或永久删除都需明确确认，减少误删风险
- 工作目录、知识库和仍被对话引用的数据会受到保护，不会被当作清理对象

## v1.1.0 LTS

**2026年7月2日**

本次升级新增 **斜杠命令系统、文件时光回退、Hooks 自动化、AI 预填建议** 等多项功能，同时全面提升聊天界面与 Dashboard 的交互体验。


### 1. 全新斜杠命令系统
- 全面重构斜杠命令，支持 `/new`（新会话）、`/stop`（停止响应）、`/data`（数据预览）、`/jobs`（任务历史）、`/compact`（压缩上下文）等系统命令
- 命令支持别名搜索、Tab 补全和参数提示；输入 `/help <命令名>` 可查看详细说明
- 新增自定义 Prompt 命令：在工作目录 `.baa/commands/` 下放置 Markdown 文件即可添加自定义命令

### 2. Hooks 用户自动化
- 新增 Hooks v1，支持在 AI 会话的关键节点（开始对话、每轮请求前后、工具调用前后）触发自动化动作
- 在应用设置中可直接编辑、校验和测试 Hooks 配置；支持 `prompt` 注入和 Python 脚本调用
- Agent 可读取公开文档或 API 说明，自动帮你生成 Hooks 配置

### 3. AI 预填下一步建议
- 每次 AI 回复完成后，自动预测并预填一条"下一步可发送的消息"到输入框
- 输入框为空时才预填，一旦开始输入、发消息或切换会话，旧建议自动失效

### 4. 工作目录时光回退
- 每次发消息前自动创建快照；工作目录中的文件被修改前也会自动备份旧版本
- `/checkpoint` 打开时间线面板，显示每个快照的触发消息和修改文件数
- 支持三种回退模式：恢复文件+对话、仅对话、仅文件，满足不同撤销需求

### 5. 工作目录管理增强
- 弹层新增历史记录列表，可一键切换到之前用过的工作目录
- 新增"移除记录"功能，从列表隐藏目录记录，本地文件全部保留
- 新增存储清理功能，自动识别并清理失效缓存，释放占用空间

---

## v1.0.0_LTS
**2026年6月20日**

首个长期支持版本（LTS），整合 v5.1 之后的全部改进，聚焦 **工作目录、分析技能、前端体验与安全加固**。

### 1. 工作目录挂载（全新）
- 可直接挂载本地文件夹作为工作目录，无需逐个上传文件即可分析其中的数据
- 挂载后自动注册目录内的 CSV / Excel 文件，效果等同批量上传，AI 可直接查询
- **秒级恢复**：关闭软件后已注册的表会保留，下次重新挂载无需重新解析，大 Excel 只解析一次
- 导出文件自动写入工作目录下的 `artifacts/` 文件夹，跟随项目统一管理
- 文件目录采用摘要、分页与按需搜索，避免大量文件撑满对话上下文

### 2. 分析技能系统
- 新增斜杠命令技能系统，可通过 `/funnel-analysis` 等命令快速调用预设分析流程
- 支持加载自定义技能，技能只注入提示词，不执行外部代码

### 3. 前端体验全面升级
- 界面核心渲染路径迁移至 Vue 框架，交互更流畅、响应更稳定
- 模型设置、知识库、MCP 工具管理三大面板重构，告别重复点击与状态错乱
- 工具执行间隙显示"正在思考下一步"等待态，不再误以为卡住
- 修复思考过程与工具步骤之间的动画空白

### 4. 历史对话管理
- 已保存对话支持重命名
- 加载历史对话时显示进度遮罩与耗时计数，可中途终止

### 5. 安全加固
- SQL 查询新增路径白名单与语法校验，防止越权读取工作目录之外的文件
- 网络地址（http / s3 等）一律拒绝，杜绝 SSRF 风险
- 文件读写强制经过工作目录鉴权

### 6. 项目结构整理
- 运行模块集中至 `infrastructure/`，用户文档与教程集中至 `Information/`

---

## v1.0
- 项目正式发布第一个稳定版本
- 支持基础的数据上传、查看、分析和可视化
- 支持 Excel、CSV、数据库等常见数据源
- 可通过对话方式完成常见数据分析任务
- 提供基础的图表生成与结果导出能力

---

## v2.0
**2026年5月11日**

本次升级主要围绕 **更强的分析能力、更一致的展示体验、更好的导出能力** 展开。

### 1. 日志系统升级
- 新增统一日志系统
- 自动记录运行过程、工具调用、模型请求与异常信息
- 日志按天轮转，保留 30 天，便于排查问题

### 2. 配色方案统一
- 修复图表和 PPT 配色不一致的问题
- 用户切换品牌配色后，图表和报告会同时生效
- 默认配色为 McKinsey，也支持 BCG 等方案

### 3. Agent 架构重构
- 将原本单文件的 Agent 拆分为多个模块
- 提升代码可维护性和扩展性
- 后续新增功能会更容易接入

### 4. 国际化支持
- 新增中英双语切换
- 前端界面可一键切换中文 / English
- 页面文案、提示、命令说明等已统一适配

### 5. 斜杠命令优化
- 将原来的通用分析命令拆分为更明确的命令
- 如：`/decile`、`/tree`、`/kmeans`
- 用户更容易理解每个命令的用途

### 6. 导出功能增强
- 新增 Excel 和 Word 报告导出
- 支持将分析结果直接生成文件并下载
- `/export` 和 `/report` 命令可快速完成导出任务

### 7. 数据清洗功能增强
- 新增数据概览、缺失值处理、缩尾、截尾等清洗能力
- 支持更灵活的数据预处理流程
- 清洗后数据可直接用于后续分析

### 8. 数据预览升级
- 原来的数据结构查看改为表格预览
- 更接近 Excel 的查看体验
- 可快速浏览各个表、字段和样本数据

### 9. Token 用量追踪
- 新增上下文窗口与输出 token 统计
- 可在界面中查看当前 token 使用情况
- 便于控制模型成本和上下文长度

### 10. 思考模式展示
- 支持展示模型的推理过程
- 在需要时可查看模型思考链
- 适合复杂分析任务的调试与理解

### 11. 前端体验优化
- 页面结构进一步拆分，维护更清晰
- 样式与脚本独立管理
- 界面交互更稳定，响应更流畅

---

## v3.0
**2026年5月18日**

本次升级聚焦于 **外部生态集成与业务知识增强**，大幅拓展了 Agent 的数据接入能力和领域适应能力。

### 1. MCP 工具调用能力
- 新增 MCP（Model Context Protocol）协议支持，Agent 可动态调用外部 MCP 工具
- 支持调用计算器、代码执行器、第三方 API 封装工具等，扩展分析能力边界
- 通过标准化协议，可接入符合 MCP 规范的任意工具生态
- 工具调用过程自动记录至日志，便于调试和审计

### 2. 业务知识库集成
- 新增业务数据库功能，支持导入企业内部的业务资料、产品文档、行业报告等
- 自动对资料进行向量化处理，构建可检索的知识库
- Agent 在执行分析时会自动检索相关知识，提升对特殊业务场景的理解和洞察能力
- 支持多种格式：Word、Excel等，满足常见文档导入需求

### 3. 数据源扩展：Google Sheets 与自定义 API
- **Google Sheets API 集成**：支持直接读取 Google Sheets 中的数据作为分析源
- **自定义数据库 API 接口**：提供通用 API 适配器
- 所有外部数据源接入后均可使用数据清洗、预览、分析等全套功能

---

## v4.0
**2026年5月29日**

本次升级聚焦于 **前端体验全面重构、图表稳定性提升与工程质量强化**。

### 1. 用户界面优化
- 侧边栏重构为三段式信息架构：状态区、操作区、历史区
- 新增**模型连接测试**指示点（选择模型后自动测试，也可在设置中手动触发）
- Agent 输出气泡改为"报告风格"：左侧品牌色竖线 + 底纹背景，与图表框视觉统一
- 支持一键切换暗黑模式
- 将 `agent_chat.css` 拆分为 `tokens`、`base`、`chat`、`modals`、`kb` 五个子模块
- 增加操作指南，包含常见问题说明

### 2. 增强稳定性
- 将图表依赖本地化，消除外部 CDN 依赖
- 修复加载历史对话时强制覆盖当前已选模型的问题：仅在用户尚未选择模型时才从历史恢复

### 3. 批量数据处理能力提升
- 改变原数据源接入后转换逻辑，从SQL Slight转换为duckdb，万行数据秒处理

### 4. 新增时间序列分析模块
- 支持Prophet 、SARIMA、ARIMA、VAR、GRU模型

---

## v5.0
**2026年6月4日**

本次为一次大版本迭代，涵盖 **多数据源、智能交互、稳定性修复与安全加固** 四大方向。

### 1. 多数据源支持
- 现在可以同时连接多个数据源，不再限制只能用一个
- 侧边栏新增数据源列表，每个来源都有独立的开关，可以自由选择激活哪些数据源参与分析
- AI 在多数据源场景下能自动识别每条查询应该去哪个数据源取数，支持跨数据源的联合分析

### 2. AI 主动提问
- 当用户的需求比较模糊时（比如没有说清楚要按哪个维度分析、用哪个指标），AI 会主动暂停并提出问题
- 界面上会出现选项卡片，点击选项即可继续；也可以选"其他"输入自定义答案
- AI 不会随意打断——只有在不同选择会导致实质不同结果时才会提问

### 3. 对话自动保存
- 每次 AI 回复完成后会自动保存对话记录，刷新页面或重新打开也不会丢失
- 已保存对话列表中，自动保存和手动保存的记录样式统一，用小标签区分，不再有视觉上的"高低之分"
- 修复了加载已保存对话后再继续聊天时，自动保存会错误创建新记录的问题

### 4. MCP 工具接入体验优化
- 已接入的 MCP 工具现在可以展开查看详细说明，了解每个工具的用途和参数
- 新增"智能填充"功能：把工具的安装命令或配置粘贴进来，自动解析填好表单，不需要手动填写各个字段
- 新增"本地扫描"功能：填入本地工具包的路径，自动识别配置信息

### 5. 数据预览升级
- 数据预览弹窗改为左右分栏布局：左侧显示所有表的列表（含行数），右侧查看具体内容
- 多数据源时左侧会按来源分组，每组显示该来源包含多少张表
- 分隔栏可以拖动调整宽度

### 6. SQL 数据库连接改善

- 连接外部数据库后，AI 查询过的表会自动缓存到本地，后续重复查询速度大幅提升，无需每次都连远端
- 修复了表数量识别不准的问题（之前大型数据库可能只识别到部分表，现在能获取全部表和视图）
- 修复了跨数据库方言（MySQL / PostgreSQL 等）的表名引号格式错误
- 大表（超过 50 万行）不再全量拉取到本地，查询直接发往远端执行，避免内存溢出

### 7. 防止 AI 编造数据

- 修复了多轮对话中 AI 会"记住"之前回复里的数字、下一轮直接复用而不重新查询的问题
- 现在 AI 能看到完整的历史查询记录，不会把文字描述当成已知事实来使用

### 8. 知识库触发修复

- 修复了业务知识库在对话中经常不被触发的问题——之前 AI 会因为各种理由跳过查知识库这一步
- 现在只要是数据分析请求，AI 会优先查阅知识库中的指标定义和业务规则，再开始分析

### 9. 其他体验修复

- AI 回复时，用户向上翻看之前的内容不会再被强制拉回底部；新消息发出时才会自动滚到最新
- 修复了 Google Sheets 数据源有时只识别到一半工作表的问题
- 修复了分组柱状图的分组字段有时不生效、退化为普通柱状图的问题
- 修复了加载已保存对话后，侧边栏数据源状态不刷新、显示旧状态或空白的问题
- 增强了安全性：AI 执行的所有 SQL 查询经过更严格的语法层检查，并在数据库连接层面关闭了文件系统和网络访问权限

---

## v5.1
**2026年6月10日**

本次升级聚焦于 **多数据源联合分析、数据接入稳定性体验**。

### 1. 跨数据源联合分析增强
- 新增多数据源虚拟合并层，支持直接对不同数据源中的表执行跨源 `JOIN` 和 `UNION`
- 当多个 Excel 或 CSV 文件包含同名表时，系统会自动为表添加数据源前缀，AI 可以准确区分并查询指定文件
- 优化多数据源查询路由，单源查询与跨源查询均可自动选择正确的执行方式
- 数据源结构信息现在会附带样本数据，帮助 AI 区分结构相同但内容不同的表

### 2. Excel 数据接入修复
- 修复包含银行账号、证件号、编码等“空格分隔数字串”的 Excel 列上传失败问题
- 普通文本列会被稳定识别为字符串，不再被 DuckDB 错误推断为数值并触发类型转换异常

### 3. 数据源断连提示优化
- 修复服务重启后前端仍显示数据源已连接、查询却无响应的问题
- 当会话中的数据源连接已经失效时，系统会明确提示用户重新连接，不再出现空白回复
- 增加前后端双重检查，避免 AI 在缺少数据结构时继续无效执行

### 4. 模型测试体验优化
- 模型设置支持“先测试、后保存”
- 用户填写新的 API Key、Base URL 或模型名称后，可直接测试当前输入内容
- 输入框为空时仍会自动使用已保存配置，兼容原有测试方式
