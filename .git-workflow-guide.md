# Git 工作流指南 - 二次开发版本管理

## 🚀 初始化设置（仅需执行一次）

### 步骤 1：Fork 原仓库到你的 GitHub 账号
1. 访问：https://github.com/Zafer-Liu/Data-Analysis-Agent
2. 点击右上角 "Fork" 按钮
3. Fork 到你的账号下

### 步骤 2：设置双远程仓库
```bash
# 查看当前远程仓库
git remote -v

# 将现有的 origin 重命名为 upstream（上游）
git remote rename origin upstream

# 添加你 Fork 的仓库作为新的 origin
git remote add origin https://github.com/你的用户名/Data-Analysis-Agent.git

# 验证配置
git remote -v
# 应该看到：
# origin    https://github.com/你的用户名/Data-Analysis-Agent.git (fetch)
# origin    https://github.com/你的用户名/Data-Analysis-Agent.git (push)
# upstream  https://github.com/Zafer-Liu/Data-Analysis-Agent.git (fetch)
# upstream  https://github.com/Zafer-Liu/Data-Analysis-Agent.git (push)
```

### 步骤 3：创建开发分支
```bash
# 创建并切换到自定义开发分支
git checkout -b custom-dev

# 推送到你的远程仓库
git push -u origin custom-dev
```

---

## 🔄 日常开发工作流

### 开发新功能
```bash
# 1. 确保在开发分支上
git checkout custom-dev

# 2. 创建功能分支（可选，推荐）
git checkout -b feature/your-feature-name

# 3. 进行开发...
# 修改代码、测试

# 4. 提交更改
git add .
git commit -m "feat: 添加XX功能"

# 5. 合并回开发分支
git checkout custom-dev
git merge feature/your-feature-name

# 6. 推送到你的远程仓库
git push origin custom-dev
```

---

## ⬇️ 拉取上游更新

### 方式 1：合并更新（推荐）
```bash
# 1. 切换到主分支
git checkout main

# 2. 拉取上游最新代码
git fetch upstream
git merge upstream/main

# 3. 推送到你的 Fork
git push origin main

# 4. 合并到开发分支
git checkout custom-dev
git merge main

# 5. 解决冲突（如果有）
# 手动编辑冲突文件
git add .
git commit -m "merge: 合并上游更新 v1.x.x"

# 6. 推送更新
git push origin custom-dev
```

### 方式 2：变基更新（保持提交历史整洁）
```bash
# 1. 获取上游更新
git fetch upstream

# 2. 在开发分支上变基
git checkout custom-dev
git rebase upstream/main

# 3. 解决冲突后继续
git rebase --continue

# 4. 强制推送（因为改写了历史）
git push origin custom-dev --force-with-lease
```

---

## 🔧 冲突解决策略

### 如果遇到冲突：
```bash
# 1. 查看冲突文件
git status

# 2. 手动编辑冲突文件，保留你的修改
# 冲突标记形式：
# <<<<<<< HEAD
# 你的代码
# =======
# 上游代码
# >>>>>>> upstream/main

# 3. 标记为已解决
git add 冲突文件.py

# 4. 继续合并/变基
git merge --continue
# 或
git rebase --continue
```

### 保留你的版本：
```bash
git checkout --ours 文件路径  # 使用你的版本
```

### 使用上游版本：
```bash
git checkout --theirs 文件路径  # 使用上游版本
```

---

## 📦 推荐的分支结构

```
main (upstream)          ← 上游原始代码（只读，仅用于同步）
  ↓
custom-dev              ← 你的主开发分支（包含所有定制）
  ↓
feature/ui-redesign     ← 具体功能分支
feature/new-datasource  ← 具体功能分支
feature/api-extension   ← 具体功能分支
```

---

## 🎯 最佳实践

1. **永远不要在 main 分支上开发**
   - main 分支仅用于同步上游
   
2. **定期同步上游**
   - 建议每周或每次上游发布新版本时同步
   
3. **提交前先拉取**
   - 推送前先 `git pull` 避免冲突
   
4. **使用有意义的提交信息**
   - 格式：`类型: 简短描述`
   - 例如：`feat: 添加新的数据源支持`、`fix: 修复图表渲染问题`
   
5. **大改动前创建备份分支**
   ```bash
   git checkout -b backup-before-merge
   git push origin backup-before-merge
   ```

---

## 🚨 紧急回滚

### 如果更新后出现问题：
```bash
# 查看提交历史
git log --oneline

# 回滚到之前的提交
git reset --hard <commit-id>

# 或者撤销最后一次合并
git reset --hard HEAD~1

# 强制推送（谨慎使用）
git push origin custom-dev --force-with-lease
```

---

## 📝 常用命令速查

```bash
# 查看当前分支
git branch

# 查看远程分支
git branch -r

# 查看所有分支
git branch -a

# 删除本地分支
git branch -d feature/old-feature

# 删除远程分支
git push origin --delete feature/old-feature

# 查看修改状态
git status

# 查看修改内容
git diff

# 暂存当前修改
git stash

# 恢复暂存
git stash pop

# 查看暂存列表
git stash list
```

---

## 🎓 学习资源

- Git 官方文档：https://git-scm.com/doc
- GitHub Flow：https://docs.github.com/en/get-started/quickstart/github-flow
- Git 分支管理：https://learngitbranching.js.org/
